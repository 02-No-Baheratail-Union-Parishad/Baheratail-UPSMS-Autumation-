# সেটআপ গাইড — প্রত্যয়নপত্র অটোমেশন (চূড়ান্ত সংস্করণ)

## ১. Google Sheet ও Apps Script তৈরি
1. একটি নতুন Google Sheet খুলুন → Extensions → Apps Script
2. প্রজেক্টে তিনটি ফাইল বানান: `Code.gs`, `Gemini.gs`, `Index.html` — এই তিনটি ফাইলের কনটেন্ট এখানে দেওয়া ফাইল থেকে কপি-পেস্ট করুন

## ২. Gemini API Key
1. Google AI Studio থেকে একটি ফ্রি Gemini API Key নিন
2. Apps Script এডিটরে ⚙️ Project Settings → Script Properties → Add script property
   - Property: `GEMINI_API_KEY`
   - Value: আপনার API Key

## ৩. Google Doc টেমপ্লেট বানান

**সহজ উপায়:** এই প্যাকেজের সাথে দেওয়া `BUP_Certificate_Template.docx` ফাইলটি সরাসরি ব্যবহার করুন —
1. Google Drive-এ ফাইলটি আপলোড করুন
2. ফাইলের উপর রাইট-ক্লিক → "Open with" → "Google Docs" (এটি স্বয়ংক্রিয়ভাবে একটি এডিটযোগ্য Google Doc-এ রূপান্তরিত হবে)
3. এই নতুন Google Doc-টিকেই `CONFIG.TEMPLATE_DOC_ID` হিসেবে ব্যবহার করুন (Doc-এর URL-এর `/d/` এবং `/edit` এর মাঝের অংশটুকু আইডি)
4. ফাইলের ভেতরে থাকা `[চেয়ারম্যানের নাম]`, `[Chairman's Name]`, `[নম্বর লিখুন]`, `[Number]` — এই বন্ধনী-করা অংশগুলো আপনার প্রকৃত চেয়ারম্যানের নাম/পদবি/মোবাইল দিয়ে একবার ম্যানুয়ালি বদলে দিন (এটা প্রতি সনদে বদলায় না, তাই একবারই এডিট করলেই চলবে)
5. মাঝখানের সিলটি (গোলাকার সবুজ লোগো) ইতিমধ্যে বসানো আছে — চাইলে আপনার অফিসের প্রকৃত সিল/লোগোর ছবি দিয়ে প্রতিস্থাপন করতে পারেন (ছবির উপর ক্লিক করে "Replace image")

টেমপ্লেটের গঠন রেফারেন্স ডিজাইন অনুযায়ী সাজানো: উপরে সরকার+ইউনিয়নের নাম, তার নিচে সিল, তারপর সনদের ধরন (বক্সে), স্মারক নং/তারিখ, আবেদনকারীর তথ্যের বক্স, AI-জেনারেটেড বিবরণী, প্রয়োজনে টেবিল (ওয়ারিশ/পারিবারিক ইত্যাদি), সবশেষে দ্বিভাষিক (বাংলা+ইংরেজি) চেয়ারম্যান তথ্য পাশাপাশি, QR কোড ও স্বাক্ষরের জায়গা। পুরো কার্ডটি ডবল বর্ডারে (ভেতরে সবুজ, বাইরে লাল রেখা) ঘেরা।

**নিজে থেকে বানাতে চাইলে**, হুবহু এই প্লেসহোল্ডারগুলো বসান (দরকার অনুযায়ী):

| প্লেসহোল্ডার | কী বসবে |
|---|---|
| `{{সনদ_নং}}` | বাংলা ইউনিক সনদ নম্বর |
| `{{ইস্যুর_তারিখ}}` | বাংলা তারিখ |
| `{{নাম}}` | আবেদনকারীর নাম |
| `{{পিতার_নাম}}` | পিতা/স্বামীর নাম |
| `{{মাতার_নাম}}` | মাতার নাম |
| `{{গ্রাম}}` | গ্রাম |
| `{{ডাকঘর}}` | ডাকঘর |
| `{{ওয়ার্ড_নং}}` | ওয়ার্ড নম্বর |
| `{{NID_Birth_No}}` | NID/জন্ম নিবন্ধন নম্বর |
| `{{প্রত্যয়নপত্রের_ধরন}}` | সনদের ধরন |
| `{{body_text}}` | Gemini-জেনারেটেড দাপ্তরিক বিবরণী |
| `{{QR_CODE}}` | যাচাইকরণ QR কোড এখানে বসবে |
| `{{TABLE:FAMILY_TABLE}}`, `{{TABLE:HEIRS_TABLE}}`, `{{TABLE:CORRECTION_TABLE}}`, `{{TABLE:NAMES_TABLE}}`, `{{TABLE:FEE_TABLE}}` | সংশ্লিষ্ট সনদের ধরনে থাকা টেবিল এখানে বসবে |

**নোট:** `Extra_Simple_Fields` (যেমন মৃত্যুর তারিখ, হোল্ডিং নং) আর আলাদা `{{key}}` প্লেসহোল্ডারে বসাতে হয় না — এগুলো এখন সরাসরি Gemini-কে দেওয়া হয় এবং `{{body_text}}`-এর বাক্যের ভেতরেই স্বাভাবিকভাবে বসে যায়। টেমপ্লেটে যেসব `{{TABLE:...}}` বা `{{key}}` প্লেসহোল্ডার কোনো নির্দিষ্ট সনদে ব্যবহৃত হয় না, সেগুলো সিস্টেম নিজে থেকেই চুপচাপ মুছে দেয় — তাই একটাই স্ট্যাটিক টেমপ্লেট সব ৪০+ ধরনের জন্য নিরাপদে ব্যবহার করা যায়।

এই Doc-টিকে "Template only" হিসেবে রাখুন — কখনো ম্যানুয়ালি এটাতে সনদ ইস্যু করবেন না, কারণ সিস্টেম প্রতিবার এটার একটি কপি বানায়।

## ৪. Drive ফোল্ডার
একটি Google Drive ফোল্ডার বানান যেখানে সব PDF/Doc জমা হবে।

## ৫. `Code.gs`-এর CONFIG আপডেট করুন
```js
const CONFIG = {
  TEMPLATE_DOC_ID: "আপনার Doc-এর ID (URL থেকে নিন)",
  TARGET_FOLDER_ID: "আপনার ফোল্ডারের ID (URL থেকে নিন)",
  ...
};
```

## ৬. Sheet প্রস্তুত করুন
Google Sheet রিলোড করুন — উপরে "🤖 ইউনিয়ন অটোমেশন" মেনু দেখা যাবে:
1. **"Certificates ও Master DB শীট প্রস্তুত করুন"** — একবার চালান
2. **"Config শীট ৪০+ সনদ দিয়ে পূরণ করুন"** — একবার চালান (নতুন সনদের ধরন পরে ম্যানুয়ালি Config শীটে সারি যোগ করে বাড়ানো যাবে)

## ৭. Web App হিসেবে ডিপ্লয় করুন
1. Deploy → New deployment → Web app
2. Execute as: **Me**
3. Who has access: **Anyone**
4. Deploy করুন, অনুমতি দিন, URL সংরক্ষণ করুন — এটিই আপনার ফর্ম ও QR-ভেরিফিকেশন উভয়ের ঠিকানা

## ৮. পরীক্ষা করুন
1. Web App URL খুলুন → ফর্ম পূরণ করে একটি টেস্ট সনদ তৈরি করুন
2. তৈরি হওয়া PDF-এর QR কোড স্ক্যান করে দেখুন — যাচাইকরণ পেজ ঠিকমতো আসছে কিনা
3. একই NID + একই ধরন দিয়ে আবার সাবমিট করে দেখুন — ডুপ্লিকেট সতর্কবার্তা আসা উচিত

## নিরাপত্তা নোট
"Anyone" অ্যাক্সেস QR-ভেরিফিকেশনের জন্য প্রয়োজনীয়, তবে এর অর্থ ফর্ম-সাবমিশন এন্ডপয়েন্টও প্রযুক্তিগতভাবে যে কেউ ডিপ্লয়মেন্ট URL জানলে কল করতে পারবে। যদি শুধু অফিস স্টাফ ফর্ম পূরণ করবে এমন সেটআপ চান, তাহলে ফর্ম ও ভেরিফিকেশনকে দুটি আলাদা ডিপ্লয়মেন্টে ভাগ করে ফর্মটির অ্যাক্সেস "Anyone within domain"-এ সীমিত রাখার কথা বিবেচনা করুন।
